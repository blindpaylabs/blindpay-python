name: Main
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    uses: ./.github/workflows/lint.yaml

  typecheck:
    needs: lint
    uses: ./.github/workflows/typecheck.yaml